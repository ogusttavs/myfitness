# Story 1.1 — Bootstrap Expo + TypeScript + expo-router

**Epic:** EPIC-1 Foundation
**Status:** Done
**Owner:** @dev
**Refs:** spec.md §4, complexity.json (CON-01)

## Goal
Inicializar o projeto Expo com TypeScript strict e expo-router file-based, rodando em iOS Simulator e Android Emulator.

## Acceptance Criteria
- [x] `package.json` com Expo SDK 54 + scripts (start, ios, android, lint, typecheck, test)
- [x] TS strict (`strict: true`, `noUncheckedIndexedAccess: true`)
- [x] `app/_layout.tsx` raiz + rota `app/index.tsx` (tela Modo Caverna)
- [x] expo-router configurado em `app.json` + `scheme: modocaverna`
- [x] Bundle ID configurado (com.gustavosilva.modocaverna)
- [x] NativeWind v4 + tailwind config + tokens
- [x] Estrutura de pastas src/{theme, lib, stores, queries, components, features}/

## Tasks
- [x] `npx create-expo-app mobile --template blank-typescript`
- [x] Adicionar deps full stack (TanStack Query, Zustand, Supabase, NativeWind, expo-notifications, etc.)
- [x] Configurar app.json (dark, scheme, plugins, bundle ID)
- [x] tsconfig strict + path aliases (@/*, @app/*)
- [x] babel.config.js + metro.config.js para NativeWind
- [x] Theme tokens em src/theme/tokens.ts
- [x] tailwind.config.js mapeando paleta Modo Caverna
- [x] Tela inicial em app/index.tsx com layout caverna
- [x] README do app + README do monorepo

## File List
- apps/mobile/package.json
- apps/mobile/app.json
- apps/mobile/tsconfig.json
- apps/mobile/babel.config.js
- apps/mobile/metro.config.js
- apps/mobile/tailwind.config.js
- apps/mobile/global.css
- apps/mobile/nativewind-env.d.ts
- apps/mobile/.env.example
- apps/mobile/app/_layout.tsx
- apps/mobile/app/index.tsx
- apps/mobile/src/theme/tokens.ts
- apps/mobile/README.md
- README.md (monorepo)

## Dev Notes
- Optei por monorepo `apps/mobile/` em vez de raiz para separar AIOX framework do código.
- `pnpm` desejado mas requer sudo via corepack — usei `npm`. Trocar via `corepack enable` (sudo) é trivial depois.
- `npm install` ainda não rodado pelo dev — primeiro passo do usuário.
- Story 1.2 (theme) e parte de 1.5 (estrutura) já adiantadas dentro desta story para evitar bootstrap quebrado.

# Story 5.4 — RestTimerRing component

**Epic:** EPIC-5
**Status:** Done (visualmente; integração de som/haptic é story 5.3)
**Owner:** @dev

## Goal
Componente UI do cronômetro: ring SVG animado + número Bebas Neue gigante + controles −15s, pause/play, +15s, pular.

## Acceptance Criteria
- [x] Ring SVG com `react-native-svg` + `Reanimated` para animar `strokeDashoffset`
- [x] Cor do ring muda: ember (normal) → glow (últimos 5s) → moss (finalizado)
- [x] Número pulsa nos últimos 5s (scale 1 → 1.06 com `withRepeat`)
- [x] Botões grandes (≥56pt), pause/play vira pause quando running
- [x] Skip em link minimal abaixo
- [x] Label "próxima: …" opcional

## File List
- apps/mobile/src/components/RestTimerRing.tsx
- apps/mobile/app/timer-demo.tsx (rota /timer-demo para testar)
- apps/mobile/app/index.tsx (link "VER TIMER")

## Dev Notes
- A engine (Story 5.1) é a fonte da verdade. Componente é puramente apresentacional.
- Story 5.3 vai plugar `expo-haptics` + `expo-audio` + `expo-notifications` no callback `onFinish` do `useTimer`.
- Animação de finish (ring vira moss + auto-fade) é trivial — adicionar quando integrar com BottomSheet do EPIC-5.
